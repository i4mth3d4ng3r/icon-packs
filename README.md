# Icon Packs

Community-maintained icon packs for media applications that support the Icon Pack JSON format.

## Format

Each pack is a JSON array of icons:

```json
[
  { "name": "Label Name", "url": "[https://example.com/icon.png](https://example.com/icon.png)" }
]
